{
    "version": 2,
    "outputDirectory": ".",
    "buildCommand": "npm install && npm run postbuild",
    "rewrites": [
      {
        "source": "/api/(.*)",
        "destination": "/server.js"
      },
      {
        "source": "/(.*)",
        "destination": "/"
      }
    ]
  }